# Supabase: Transaction pooler (port 6543) — use for Next.js / Vercel
DATABASE_URL=

# Optional: direct Postgres URI (port 5432) for `npm run db:push` only if push crashes
# DIRECT_DATABASE_URL=

# Min 16 characters; use a long random string in production
JWT_SECRET=
